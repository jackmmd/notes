mkdir -p /home/thor/asia/india/bangalore => Crear directorios en cadena si no existen
mkdir {nmap,content,exploits} => Crea varias carpetas
cp -r folder1 folder2 => Esto copia una carpeta dentro de la otra con todos sus archivos no solo los archivos
id thor=> Ver el id de un usuario en linux
sudo cat /etc/*release*
cat /etc/system-release => Para saber que sistema opertativo se esta ejecutando en el servidor
cat /etc/os-release => Para saber la version del sistema operativo

nslookup domain.com => Resolver el dng
ping domain.com => Resolver el DNS
~/.zshrc => Se ejecuta cada vez que se habre una nueva terminal
source ~/.zshrc => Sirve para recargar el archivo y aplicar los cambios

ping -c 1 1.0.0.0 => Hacer un ping
TTL => linux 64 - Windows 128
